"""A staged implementation of a proactive email agent."""

import sys

if sys.version_info < (3, 11):
    raise SystemExit("Python 3.11+ is required. Check python3 --version and select a newer interpreter.")
